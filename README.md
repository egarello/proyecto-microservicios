Buildear y levantar el docker-compose -> te ejecuta todos los microservicios y el eureka server.
Para esto: ejecutar ./mvnw clean package dentro de cada servicio (eureka, gateway, usuario-server y el facultad-service)

Luego, para el front, primero asegurarse de tener instaladas las dependencias del package.json: npm install
